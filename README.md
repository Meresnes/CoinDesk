# CoinDesk

Веб-приложение для отслеживания, поиска и анализа криптовалютных активов в реальном времени.

## 🚀 Возможности

- 📊 Просмотр списка криптовалют с пагинацией
- 🔍 Поиск активов по названию
- 📈 Детальная информация о каждой монете
- 📉 Интерактивные графики истории цен

## 🛠 Технологический стек

- **Frontend**: React 19 + TypeScript
- **State Management**: Redux Toolkit + RTK Query
- **UI Framework**: Chakra UI v3
- **Routing**: React Router v7
- **Build Tool**: Vite
- **Styling**: SCSS Modules
- **Package Manager**: Yarn

## 📋 Требования

- Node.js >= 18.x
- Yarn >= 1.22.x

## 🔧 Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd CoinDesk
```

2. Установите зависимости:
```bash
yarn install
```

## 🏃 Запуск проекта

### Режим разработки
```bash
yarn dev
```

Приложение будет доступно по адресу `http://localhost:8080`

### Сборка для продакшена
```bash
yarn build
```

### Предпросмотр продакшен сборки
```bash
yarn preview
```

### Проверка кода линтером
```bash
yarn lint
```

### Автоисправление ошибок линтера
```bash
yarn lint:fix
```

## 📁 Структура проекта

```
src/app/
├── components/      # Общие компоненты
│   ├── NavBar.tsx
│   └── TopListWidget.tsx
├── pages/          # Страницы приложения
│   ├── coin_page/  # Страница деталей монеты
│   └── list_page/  # Страница списка активов
├── services/       # RTK Query API сервисы
│   └── assetService.ts
├── store/          # Redux хранилище и слайсы
│   └── coinPageSlice.ts
├── hooks/          # Кастомные хуки
├── types/          # TypeScript типы
├── utils/          # Утилитарные функции
└── styles/         # Глобальные стили и переменные
```

## 🗺 Маршруты

- `/` — редирект на `/list`
- `/list` — страница списка активов с поиском и пагинацией
- `/coin/:name` — страница деталей монеты с графиками и статистикой

## 📚 Документация

Подробная документация по архитектуре проекта находится в [docs/architecture.md](docs/architecture.md)

## 🤝 Разработка

### Основные команды

- `yarn dev` - запуск dev-сервера
- `yarn build` - сборка проекта
- `yarn preview` - предпросмотр сборки
- `yarn lint` - проверка кода линтером
- `yarn lint:fix` - автоисправление ошибок линтера

### Правила разработки

- Используйте **Yarn** для управления зависимостями (не npm/pnpm)
- Следуйте архитектурным принципам из [docs/architecture.md](docs/architecture.md)
- Используйте типизированные хуки `useAppDispatch` и `useAppSelector`
- Все API запросы через RTK Query
- SCSS Modules для семантических стилей, Chakra пропсы для композиции

## 📝 Лицензия

MIT
